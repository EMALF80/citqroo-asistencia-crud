{
  "name": "citqroo-asistencia-crud",
  "version": "1.0.0",
  "description": "Proyecto final CRUD con Vue.js y API json-server para control de asistencia del CITQROO.",
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "api": "json-server --watch db.json --port 3001",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1"
  },
  "dependencies": {
    "vue": "^3.5.22"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^6.0.7",
    "json-server": "^0.17.4",
    "vite": "^8.0.16"
  }
}

